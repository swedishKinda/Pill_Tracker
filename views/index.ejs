<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/flexboxgrid/6.3.1/flexboxgrid.min.css"
      type="text/css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" type="text/css" href="/styles/style.css" />
    <title>Pill Tracker</title>
  </head>
  <body>
    <div id="container">
      <h1 class="text-center mt-3">Pill Tracker</h1>

      <section class="row center-xs">
        <div class="col-md-6 col-xs-12 mt-5 first-md last-xs">
          <p><strong>Add medication, amount, and doses per day here</strong></p>
          <form action="/add" method="POST">
            <div class="form-group">
              <input
                class="form-control w-75 d-inline"
                type="text"
                id="name"
                name="name"
                placeholder="Name"
                required
              />
              <input
                class="form-control w-75 d-inline mt-3"
                type="number"
                id="amount"
                name="amount"
                placeholder="Amount"
                required
              />
              <input
                class="form-control w-75 d-inline mt-3"
                type="number"
                id="doses_per_day"
                name="doses_per_day"
                placeholder="Doses per day"
                required
              />
            </div>
            <button class="mt-3 btn btn-primary" type="submit">Submit</button>
          </form>
          <p class="mt-5">
            <strong>Delete medication by entering it’s name here</strong>
          </p>
          <form action="/delete" method="POST">
            <div class="form-group">
              <input
                class="form-control w-75 d-inline"
                type="text"
                id="name"
                name="name"
                placeholder="Name"
                required
              />
            </div>
            <button class="mt-3 btn btn-primary" type="submit">Submit</button>
          </form>
          <form method="POST" action="/clear-all" class="mt-5 mb-3">
            <button type="submit" class="btn btn-danger">Clear All</button>
          </form>
        </div>
        <div class="col-md-6 col-xs-12 mt-3 last-md first-xs">
          <!-- Table to display data -->
          <table class="w-100 table">
            <thead>
              <tr>
                <!-- Assuming your table has columns like 'id', 'name', 'age'. Modify accordingly -->
                <th>Name</th>
                <th>Amount</th>
                <th>Dose per day</th>
              </tr>
            </thead>
            <tbody>
              <!-- Loop through the rows and display each in a table row -->
              <% rows.forEach(row => { %>
              <tr>
                <td><%= row.name %></td>
                <td><%= row.amount %></td>
                <td><%= row.doses_per_day %></td>
              </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      </section>
    </div>
  </body>
</html>
